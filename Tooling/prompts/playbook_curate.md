# Playbook curation

The playbook for this problem is at capacity (10 entries). A new
candidate idiom from a just-proven strategy is below. Decide whether
the candidate is more useful for future agents than the WEAKEST of
the existing entries.

"More useful" means: covers a more common goal pattern, captures a
non-obvious idiom, or replaces a redundant existing entry.

# Existing playbook (10 entries, numbered)

{{EXISTING}}

# New candidate

{{CANDIDATE}}

# Your decision

Reply with EXACTLY ONE LINE in one of two forms:

- `REPLACE n` where n ∈ [1, 10]: the candidate is more useful than
  entry n. Replace entry n with the candidate.
- `KEEP`: every existing entry is more useful than the candidate.
  Discard the candidate.

Single line, no other text, no explanation. Single token format —
either the literal word `KEEP` or `REPLACE <integer>`.
