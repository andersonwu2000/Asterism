-- Imports the winding-number definition from its origin problem.
import Mathlib
import Problems.Residue.winding_number_int.Defs

-- Re-export so this problem's statement can reference `Complex.windingNumber`
-- via auto-prepended `Problems.Residue.winding_number_circle.Defs`.
