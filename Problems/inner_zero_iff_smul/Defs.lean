import Mathlib

/-!
Shared definitions for the inner_zero_iff_smul problem (HW C 泛函 2).
No problem-specific defs — the statement uses Mathlib's `inner` and
real-vector arithmetic directly.
-/

namespace Problems.inner_zero_iff_smul
end Problems.inner_zero_iff_smul
