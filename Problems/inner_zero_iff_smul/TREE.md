# inner_zero_iff_smul — TREE

_Auto-updated by dispatcher on every cascade._

```
main  (proved, attempts=3)
└── via s161  (succeeded)
    ├── s161_sub_1  (proved, attempts=1)
    ├── s161_sub_2  (proved, attempts=1)
    └── s161_sub_3  (proved, attempts=1)
```

**Counters:** 4 proved
