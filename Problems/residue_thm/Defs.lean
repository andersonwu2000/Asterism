-- residue_thm reuses windingNumber + residue defs from the Residue chain.
-- Both definitions live in the problems that introduce them; this file
-- re-imports them so the framework's auto-prepended local Defs covers
-- the Manifest statement's symbol references.
import Mathlib
import Problems.Residue.winding_number_int.Defs
import Problems.Residue.single_pole_residue.Defs
