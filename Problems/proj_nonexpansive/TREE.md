# proj_nonexpansive — TREE

_Auto-updated by dispatcher on every cascade._

```
main  (attempting, attempts=1)
└── via s12  (proposed)
    ├── inner_sq_le_inner_of_proj  (open)
    ├── norm_le_of_sq_le_inner  (open)
    └── projector_variational_ineq  (open)
```

**Counters:** 1 attempting / 3 open
