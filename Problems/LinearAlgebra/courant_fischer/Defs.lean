import Mathlib

namespace Problems.LinearAlgebra.courant_fischer

-- No problem-specific definitions: the statement is expressed entirely in
-- mathlib vocabulary (`LinearMap.IsSymmetric.eigenvalues`, inner product,
-- `Submodule.finrank`). This file exists to satisfy the framework's
-- Defs/Root convention.

end Problems.LinearAlgebra.courant_fischer
