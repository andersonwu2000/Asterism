import Mathlib

namespace Problems.LinearAlgebra.eckart_young

-- No problem-specific definitions: the statement uses mathlib vocabulary
-- (`LinearMap.singularValues`, `LinearMap.toContinuousLinearMap` operator
-- norm, `LinearMap.range`, `IsLeast`). This file satisfies the Defs/Root
-- convention.

end Problems.LinearAlgebra.eckart_young
