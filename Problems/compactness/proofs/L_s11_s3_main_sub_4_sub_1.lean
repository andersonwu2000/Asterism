import Mathlib
import Problems.compactness.Defs

namespace Problems.compactness

theorem s11_s3_main_sub_4_sub_1 :
    ∀ {α : Type} (M : Set (PropForm α)),
    ∃ v : Valuation α, ∀ a : α, v a = true ↔ PropForm.atom a ∈ M := by
  sorry

end Problems.compactness
