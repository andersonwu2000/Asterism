<!-- Lessons learned across spawns on this problem.
     One sentence per `- ` bullet. Reflection spawn appends
     below this header; the divider line below is the
     anchor Edit tool relies on. -->

<!-- LESSONS_BEGIN -->
- `(trivializationAt F (formBundleCore I k).Fiber x₀ ⟨x, z⟩).2 = formCoordChange I k (achart H x) (achart H x₀) x z` is definitional (`rfl`) via `VectorBundleCore.localTriv_apply`; rewriting inside anonymous constructors `⟨x, …⟩` blocks `rw`/`simp` — hoist `z` out first with `have step : ∀ z, (triv ⟨x, z⟩).2 = f z := fun _ => rfl; rw [step]`, then rewrite freely.
- `extDerivWithin_pullback` along a chart transition needs no sub-goals for its side conditions: `minSmoothness ℝ 2 ≤ ∞` closes by `simp only [minSmoothness_of_isRCLikeNormedField]; exact ENat.LEInfty.out`, `MapsTo f (range I) (range I)` by the explicit witness `⟨chartAt H x ((extChartAt I x₀).symm y), rfl⟩` (extChartAt is defeq `I ∘ chartAt`), transition smoothness is the direct Mathlib cite `contDiffWithinAt_ext_coord_change` (trans-source membership via `PartialEquiv.trans_source` + `map_source` + `left_inv`), and the rest is `I.uniqueDiffOn` / `I.range_subset_closure_interior`.
- Goals of the form `extDerivWithin form s y = extDerivWithin form t y` (set-change, `t ∈ nhdsWithin y s`) have no dedicated lemma — close with `simp only [extDerivWithin]; congr 1; exact fderivWithin_of_mem_nhdsWithin hst hs hform` (`FDeriv/Basic.lean:500`).
- Citing an already-proved sibling (e.g. `form_in_coord_smooth`) in a Backward patch.lean fails lake build with "Unknown identifier" unless you write `import Problems.Geometry.stokes_mextderiv.proofs.L_<slug>` yourself — the framework auto-appends imports only for the strategy's own newly registered `new_<slug>` sub-goals (cf. explicit proofs imports in `_strategy_s11690/s11692.lean`).
- Chart-transition goals containing `fderivWithin ℝ (↑(extChartAt I x₀) ∘ ↑(extChartAt I x).symm) (Set.range I) y` need no chain rule or UniqueDiffOn side conditions: rewrite that factor to `(tangentBundleCore I M).coordChange (achart H x) (achart H x₀) p` via `tangentBundleCore_coordChange_achart` (after `(extChartAt I x).right_inv` turns `y` into `extChartAt I x p`), then `VectorBundleCore.coordChange_comp` plus `form_in_coord_eq_coord_change` on both sides closes the pullback identity sorry-free.
- Goals mentioning `formCoordChange` need an extra `open Library.Geometry.Manifold.FormCoordChange` (Defs.lean only opens `DiffFormBundle`), and rewriting a base-point equality like `(extChartAt I x).symm (extChartAt I x p) = p` inside a fiber-typed read `… q (φ q)` is blocked for `rw`/`simp` (point occurs in the fiber type) — transport it with `congrArg (fun q : M => Trivialization.continuousLinearMapAt ℝ e q (φ q))` instead, which is well-typed because the form fiber is constant.
- In a Backward patch.lean the live LSP reports "Unknown identifier" for cited `new_<slug>` sub-goals (the framework only appends `import …proofs.L_<slug>` at commit, cf. `_strategy_s11685.lean`) — verify the combinator instead with one `validate_file` containing the sub-goal stubs inlined (primed names) above the strategy body; also state trivialized-read sub-goals as the raw `(e ⟨x, s x⟩).2` form that `Bundle.Trivialization.contMDiffAt_section_iff` emits, not the `continuousLinearMapAt` wrapper, to avoid a bridging hop.
- `(trivializationAt _ (formBundleCore I k).Fiber x₀).baseSet` is defeq to `(chartAt H x₀).source` (`subset_rfl` proves the inclusion), and `Trivialization.continuousLinearMapAt ℝ e p (φ p)` rewrites to the raw read `(e ⟨p, φ p⟩).2` via `Trivialization.continuousLinearMapAt_apply_of_mem ℝ` (R explicit) — combined with `φ.contMDiff.contMDiffOn` + `Trivialization.contMDiffOn_section_iff`, trivialized-section smoothness goals close sorry-free.
- To bridge `ContDiffOn ℝ ∞ f (extChartAt I x).target` to `DifferentiableWithinAt ℝ f (Set.range I) (extChartAt I x x)`, use `(h (extChartAt I x x) (mem_extChartAt_target x)).differentiableWithinAt (by norm_num)` then `.mono_of_mem_nhdsWithin (extChartAt_target_mem_nhdsWithin x)`; note `ContDiffWithinAt.differentiableWithinAt` now expects `∞ ≠ 0` (not `1 ≤ n`) — `by norm_num` closes it.
- Locality goals `extDerivWithin ω (range I) (extChartAt I x₀ x) = extDerivWithin ω' (range I) (extChartAt I x₀ x)` with ω = ω' near the point close sorry-free via `Filter.EventuallyEq.extDerivWithin_eq` + `filter_upwards` fed by `extChartAt_target_mem_nhdsWithin' hx_src` and `nhdsWithin_le_nhds (extChartAt_preimage_mem_nhds' hx_src ((chartAt H x).open_source.mem_nhds (mem_chart_source H x)))`; the `𝓝[·]` notation additionally needs `open scoped Topology` (missing from the usual `Manifold Bundle ContDiff` open list).
