-- d-naturality of the flat pullback as a DiffForm equality.
-- Two forms agree iff their chart-coordinate representatives agree (section_eq_of_coord);
-- the LHS coord-rep is computed by coord_rep_mext_pullback (mextDeriv ∘ pullback = the flat
-- pullback formula for extDeriv φ), and the RHS coord-rep by coord_rep_pullback at extDeriv φ.
import Mathlib
import Problems.Geometry.pullback_flat_dnat.Defs
import Problems.Geometry.pullback_flat_dnat.proofs._strategy_s17815

namespace Problems.Geometry.pullback_flat_dnat

def main := @Problems.Geometry.pullback_flat_dnat.s17815

end Problems.Geometry.pullback_flat_dnat
