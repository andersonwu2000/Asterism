/-
  Defs.lean — no new vocabulary; cites the boundary chart data from
  `Library.Geometry.ManifoldBoundary` (`Defs` for the chart maps, `CompactBdry`
  for `Bdry`, `HalfSpaceFrontier` for `faceEmbed`). The topological laws of the
  chart (open source/target, continuity both ways) are the Root.
-/
import Mathlib
import Library.Geometry.ManifoldBoundary.Defs

open scoped Manifold Bundle ContDiff
open Bundle
open Library.Geometry.ManifoldBoundary.CompactBdry
open Library.Geometry.ManifoldBoundary.HalfSpaceFrontier
open Library.Geometry.ManifoldBoundary.Defs

namespace Problems.Geometry.stokes_bdry_chart_topo

end Problems.Geometry.stokes_bdry_chart_topo
