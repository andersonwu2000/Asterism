-- Split on finrank: 0 ⇒ F subsingleton ⇒ x = 0, both sides 0 (handled inline);
-- 1 ⇒ f is the scalar det f (single hard leaf). Combined by Nat.eq_zero_or_pos.
import Mathlib
import Problems.Geometry.banach_tarski.Defs
import Problems.Geometry.banach_tarski.proofs._strategy_s11425

namespace Problems.Geometry.banach_tarski

def endo_finrank_le_one_eq_det_smul := @Problems.Geometry.banach_tarski.s11425

end Problems.Geometry.banach_tarski
