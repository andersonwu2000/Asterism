- **ZMod.val negation in prime fields**: Establish positivity precondition via sub-goal chain (Prime → 2≤p → 0<p) before invoking ZMod.val_neg_one

- **ZMod algebraic bridge with value conversion**: Use ZMod p for native algebraic reasoning on factorial congruence, extract element values via `.val`, provide explicit bridge lemma relating ZMod.val to Nat.%, chain rewrites through all three.
