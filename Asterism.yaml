# Asterism project config. All fields optional; missing keys fall back
# to env vars then built-in defaults. See docs/architecture.md §10 for
# the full resolution chain (env > Asterism.yaml > legacy env > built-in).
#
# Values shown are the framework's built-in defaults — uncomment + edit
# any entry to override. Leaving the file as-is is identical to deleting
# it; the framework only reads what's set.

dispatch:
  pool: 12               # ThreadPoolExecutor workers
#   budget_sec: 1800      # daemon wall-clock cutoff (30 min)
#   shelve_threshold: 8   # total attempts per goal before shelving
#                         # (goal-level cap; spans Builder + Backward)

# builder:
#   provider: claude      # claude | gemini | openai
#   model: claude-sonnet-4-6
#   threshold: 3          # Builder attempts per goal before escalating
#                         # to Backward. Lives next to the Builder model
#                         # because it should be sized to that model:
#                         #   Sonnet/Opus:        3        ← default
#                         #   Haiku/Flash/mini:   5        (weaker; needs more shots)
#                         #   Local/quantized:    8

# backward:
#   provider: claude
#   model: claude-sonnet-4-6
#                         # backward has no `threshold` — it's the
#                         # destination kind once Builder gives up.
