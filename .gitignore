spike_temp/
*.pyc
__pycache__/

# pip install -e build artifacts
*.egg-info/

# Lake build artifacts (long-term runtime env)
/.lake
asterism.db
asterism.db-journal
asterism.db-wal
asterism.db-shm

# Per-Problem runtime state (Problems are demo / smoke / acceptance
# artifacts; commit specific Problems intentionally if needed)
/Problems/
/Library/

# Orchestrator AutoWorkspace / AutoDev removed — operational scratch +
# multi-agent framework definitions no longer needed (project past P7
# implementation phase; future audits can spawn ad-hoc).
