[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "asterism"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
asterism = "Tooling.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["Tooling*"]
